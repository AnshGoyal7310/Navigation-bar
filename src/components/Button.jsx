const Button = () => {
  return (
    <div>
      <button className="bg-primary text-white px-6 py-2 rounded-full cursor-pointer duration-300 hover:rotate-1 hover:bg-blue-800">
        Get Started
      </button>
    </div>
  );
};

export default Button;
